@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@using X.PagedList.Mvc.Core;
@using X.PagedList;
@using X.PagedList.Mvc.Core.Common;
@model IPagedList<YourTour.Models.ViewModels.KinhNghiemDuLichViewModel>
@{
    ViewData["Title"] = "Home Page";
    Layout = "~/Views/Shared/_LayoutTour.cshtml";
}
<h2 class="text-capitalize" style="color:red; text-align:center">kinh nghiệm du lịch</h2>
<div class="container py-lg-4 py-sm-3">
    @foreach (var item in Model)
    {
        <div class="row">
            <div class="col-lg-3 col-sm-12 mb-5">
                <div class="image-tour position-relative">
                    <img src="@Url.Content("/images/kinh nghiem du lich/"+item.Hinhanh)" alt="" height="200" width="600">
                </div>
            </div>
            <div class="col-lg-9 col-sm-12 mb-5">
                <h5 class="my-2" style="color:black">@item.Tieude</h5>
                <h6 class="mt-1" title="@item.Noidung" style="color:gray">@item.Noidung</h6>
                <h6 class="pagination justify-content-end"><a href="#" style="color:red;">Xem thêm <span class="fa fa-long-arrow-right"></span></a></h6>
            </div>
        </div>
    }
    <div class="pagination justify-content-end" style="margin:10px 0">
        @Html.PagedListPager(Model, page => Url.Action("KinhNghiemDuLich", new { page }),
        new PagedListRenderOptions
        {
            LiElementClasses = new string[] { "page-item" },
            PageClasses = new string[] { "page-link" }
        })
    </div>
</div>
